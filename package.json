{
  "name": "tt-cashregister",
  "version": "0.0.1",
  "description": "Cash register",
  "main": "index.html",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "lint": "polymer lint --input tt-cashregister.html",
    "dev": "polymer serve -p 8080 & npm run watch",
    "test": "polymer test",
    "watch": "browser-sync start --proxy localhost:8080 --startPath '/components/tt-cashregister/demo/' --files '*.html, *.js, demo/*' "
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.16.0"
  }
}
